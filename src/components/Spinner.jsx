import React from 'react';

const Spinner = () => (
  <div className="spinner">
    {/* Add your spinner animation or component here */}
    <div className="spinner-animation" />
  </div>
);

export default Spinner;